import java.util.*;
public class NotCandyLand {
	public static void main (String [] args) {
		Scanner userInput = new Scanner(System.in); 
		Player playerA = new Player(); 
		Player playerB = new Player();
		for (int i = 0; i < 5; i++) {
		System.out.println("Input dice rolls here. All at once please.");
		int firstRoll = userInput.nextInt();
			while (roll != 0) {
				
				
			}
		}
		
	}
	public static int getRolls(int die) {
		if (die > 0 && die < 9) {
		}
		else {
			die = 0;
		}
		
	}
}

	
